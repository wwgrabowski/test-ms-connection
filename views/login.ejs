<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - My Node.js Web App</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .login-container {
            max-width: 400px;
            margin: 50px auto;
            padding: 20px;
            background-color: #f9f9f9;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        
        .login-button {
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #0078d4; /* Microsoft blue */
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            font-size: 16px;
            margin-top: 20px;
        }
        
        .login-button:hover {
            background-color: #106ebe;
        }
        
        .dev-notice {
            margin-top: 20px;
            padding: 10px;
            background-color: #fffacd;
            border-left: 4px solid #ffd700;
            color: #666;
        }
    </style>
</head>
<body>
    <header>
        <h1>My Node.js Web App</h1>
    </header>
    
    <main>
        <div class="login-container">
            <h2>Sign In</h2>
            <p>Sign in using your organizational account to access the dashboard.</p>
            
            <a href="/login" class="login-button">
                Sign in with Microsoft
            </a>
            
            <% if (isDevelopment && mockEnabled) { %>
            <div class="dev-notice">
                <p><strong>Development Mode:</strong> Mock authentication is enabled.</p>
                <p>You will be signed in as: <%= mockUser.name %> (<%= mockUser.email %>)</p>
            </div>
            <% } %>
        </div>
    </main>
    
    <footer>
        <p>&copy; 2025 My Node.js Web App</p>
    </footer>
</body>
</html>